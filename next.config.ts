import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "images.unsplash.com",
      "nuamybopwmunwxwmrzgs.supabase.co",
    ],
  },
};

export default nextConfig;
